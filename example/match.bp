"hi",2
match {
  pat ==4 => "four"
  pat ==5 => "five"
  pat n:float => "float bound to n"
  pat :int => "int, no binding"
  pat x,y => "compound match"
  pat x, ==5, z => "coolmatch"
  pat >2,<2 => "first elem >2 and next less than"
  pat { TEST } => "grouped expr"
  pat struct {
    val x := ELEM1
    val ==5 := ELEM2
    val < 3 := ELEM3
    val struct {
      val y && ==2 := sauce
    } := ..
  } => dofunction(x)
}
