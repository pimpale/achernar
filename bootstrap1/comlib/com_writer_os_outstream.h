#ifndef COM_WRITER_OS_OUTSTREAM_H
#define COM_WRITER_OS_OUTSTREAM_H

// creates a writer that will output to the os's out stream

#include "com_writer.h"

com_writer com_writer_os_outstream_create(void);

#endif
